// Vercel entry point. The implementation stays in functions/ so it can also
// be reused by the existing serverless deployment configuration.
export { default } from '../functions/amazonLive.js';
***
